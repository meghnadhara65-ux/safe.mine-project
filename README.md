# safe.mine-project
This project is a backend training script for an AI Rockfall Prediction system. It generates synthetic sensor data (slope, vibration, rain) and trains an LSTM neural network to classify risk levels into Safe, Caution, or Imminent. It includes a sliding-window simulation to test real-time alerts in a console-based environment.
